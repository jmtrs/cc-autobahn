{
  "name": "cc-autobahn",
  "private": true,
  "version": "0.8.1",
  "type": "module",
  "description": "Mercedes W203 instrument cluster for Claude Code and Codex usage.",
  "scripts": {
    "dev": "vite",
    "test:frontend": "node --test src/modules/*.test.js",
    "test:visual": "node scripts/visual-regression.mjs",
    "capture:visual-baseline": "node scripts/visual-regression.mjs --update",
    "capture:visual-states": "node scripts/visual-regression.mjs --update-states",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "release": "node scripts/release.mjs",
    "screenshot:permission-gate": "node scripts/screenshot-permission-gate.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "playwright": "^1.61.1",
    "vite": "^8.1.5"
  }
}
