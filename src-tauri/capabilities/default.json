{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capabilities for the cluster window: listen to engine events. Window show/hide/position is handled entirely in Rust (tray click / blur), never via IPC (D24).",
  "windows": ["cluster"],
  "permissions": ["core:default", "core:event:default"]
}
